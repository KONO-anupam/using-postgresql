const {Router} = require('express');

usersController = require('./controllers/usersController')
usersRouter = Router();

usersRouter.get('/', usersController.getAllUserNames);
usersRouter.get('/new', userController.getNewUsername);
usersRouter.post('/new', userController.postNewUsername);


modules.export = usersRouter;
